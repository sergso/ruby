<%= form_with(model: r_station, local: true) do |form| %>
  <% if r_station.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(r_station.errors.count, "error") %> prohibited this r_station from being saved:</h2>

      <ul>
        <% r_station.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
